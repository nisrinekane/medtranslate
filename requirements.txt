fastapi==0.115.0
uvicorn==0.30.0
faster-whisper==1.1.0
python-multipart==0.0.12
httpx==0.28.1

# windows-only, ctranslate2 needs these to find cuBLAS / cuDNN at inference time
nvidia-cublas-cu12; sys_platform == "win32"
nvidia-cudnn-cu12; sys_platform == "win32"